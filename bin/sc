#!/usr/bin/env python

import arguments
import config

parsed_args = arguments.parse()


cfg = config.Config()
if hasattr(parsed_args, 'config') and parsed_args.config != None:
    cfg.parseConfig(file=parsed_args.config)
else:
    cfg.parseConfig()

arguments.callAction(parsed_args)
